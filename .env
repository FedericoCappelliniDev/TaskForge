# Docker Compose file merging (prod overlay)
# This makes `docker compose` automatically use both files.
# Override per-session for dev: $env:COMPOSE_FILE = "docker-compose.yml:docker-compose.dev.yml"
COMPOSE_FILE=docker-compose.yml;docker-compose.prod.yml
